# GeneticAlgorithm-Shortest-Path-in-a-maze-
Finding the shortest path in a maze using greedy genetic algorithm
Note:

For results of gentic algorithm [a,b,c] implies the route is:
                             source -> a -> b -> c -> destination
